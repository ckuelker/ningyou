#!/usr/bin/env perl -CSDA
use strict;
use Ningyou;
use Ningyou::Options;
use warnings;
our $VERSION = '0.0.4';

my $no = Ningyou::Options->new();
my $n = Ningyou->new( options => $no->get_options );
$n->run();

=pod

=head1 NAME

ningyou

=head1 USAGE

 ningyou [OPTIONS] <MODE> <SCOPE>
 
 MODE:  [show|script|install|production|full-show|full-script]
 SCOPE: [NAME OF MODULE|all]

=head1 OPTIONS

 ningyou --help
 ningyou --man
 ningyou --version
 ningyou MODE MODULE --[c <FILE>|verbose|debug [FILE]]

MODE Example:

 ningyou [OPTIONS] show        all : show what would be done currently
 ningyou [OPTIONS] full-show   all : show what would be done on a fresh system
 ningyou [OPTIONS] script      all : print the current commands as script
 ningyou [OPTIONS] full-script all : print the all commands as script
 ningyou [OPTIONS] install     all : executes commands mostly interactivily
 ningyou [OPTIONS] production  all : executes commands automatically

MODULE Example:

 ningyou [OPTIONS] show all : show what would be done for all modules
 ningyou [OPTIONS] show vim : show what would be done for the vim module

=head2 --configuration <FILE> |-c <FILE>

Example:

 -c /home/c/g/ningyou/debian-wheezy

=head2 --debug [FILE]

Example:

  --debug /var/log/ningyou.log

=head2 --help

This page

=head2 --quite

=head2 --version

=head1 BUGS

- Some output on by default is a 'feature'. However you can use --quite

=cut
