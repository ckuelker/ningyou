#!/usr/bin/env perl -CSDA
use strict;
use Ningyou;
use Ningyou::Options;
use warnings;
our $VERSION = '0.0.2';

my $no = Ningyou::Options->new();
my $n = Ningyou->new( options => $no->get_options );
$n->run();

=pod

=head1 NAME

ningyou

=head1 USAGE

 ningyou [show|script|install|production|full-show|full-script] [OPTIONS]

=head1 OPTIONS

 ningyou --help
 ningyou --man
 ningyou --version
 ningyou MODE MODULE --[c <FILE>|verbose|debug [FILE]]

MODE Example:

 ningyou show        all [OPTION] : show what would be done currently
 ningyou full-show   all [OPTION] : show what would be done on a fresh system
 ningyou script      all [OPTION] : print the current commands as script
 ningyou full-script all [OPTION] : print the all commands as script
 ningyou install     all [OPTION] : executes commands mostly interactivily
 ningyou production  all [OPTION] : executes commands automatically

MODULE Example:

 ningyou show all [OPTION] : show what would be done for all modules
 ningyou show vim [OPTION] : show what would be done for the vim module

=head2 --configuration <FILE> |-c <FILE>

Example:

 -c /home/c/g/ningyou/debian-wheezy

=head2 --debug [FILE]

Example:

  --debug /var/log/ningyou.log

=head2 --help

This page

=head2 --quite

=head2 --version

=head1 BUGS

- Some output on by default is a 'feature'. However you can use --quite

=cut
